config {
    profile ('HTTP Live Stream') {
	def HLS_PLAYER = '/path/to/hls-wrapper'

        pattern {
            match uri: '\\.m3u8$'
        }

        action {
	    executable = HLS_PLAYER
	    args = [ uri, output ]
        }
    }
}
